export enum SHIFTS_FILTER {
    ALL,
    ME
}

export enum STATUS_CODE {
    WAITING = "ממתין", 
    REFUSED = "לא מאושר", 
    CONFIRMED = "מאושר"
}

export enum STATUS_CODE_NUMBER {
    WAITING, 
    REFUSED, 
    CONFIRMED
}